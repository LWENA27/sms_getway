<!-- SMS Gateway AndroidManifest.xml Configuration -->
<!-- 
Place this in: android/app/src/main/AndroidManifest.xml

Add these permissions inside the <manifest> tag (before the <application> tag)
-->

<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required Permissions for SMS Gateway -->
    
    <!-- Send SMS Messages -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    
    <!-- Read SMS Messages -->
    <uses-permission android:name="android.permission.READ_SMS" />
    
    <!-- Receive SMS Delivery Reports -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    
    <!-- Access Phone State (for phone number, etc.) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    
    <!-- Internet Access (for Supabase) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Read Contacts (optional, for contact import) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- Write External Storage (optional, for CSV import) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <!-- Required Features -->
    <uses-feature android:name="android.hardware.telephony" android:required="true" />
    
    <application>
        <!-- Your activities here -->
    </application>
</manifest>
